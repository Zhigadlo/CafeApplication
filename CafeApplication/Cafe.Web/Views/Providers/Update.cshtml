@using Cafe.Domain
@model Provider
@{
    ViewData["Title"] = "Provider update";
}
@using (Html.BeginForm("UpdateView", "Providers", FormMethod.Post))
{
        <h3>Ingridient update</h3>
        <p>
            <label>Name</label><br />
            <input type="text" name="Name" value="@Model.Name" required/>
        </p>
        <p>
            <input type="submit" value="Update">
        </p>
}